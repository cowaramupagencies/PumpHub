import type { PumpFamily, PumpModel } from "@/types";

/**
 * Placeholder for non-Ricambio extended families.
 * EvoDrive now lives in evo-seed.ts (generated from Ricambio CE_564).
 */
export const extendedSeedFamilies: PumpFamily[] = [];

export const extendedSeedModels: PumpModel[] = [];
